#include<iostream>
using namespace std;

int main() {

    cout<<"I am shubham lodhi a Future MNNIT allahbad student"  << endl ;

    int a = 7677 ;

    // only use single quotes in (char) function and only single letter  because it is only of 1 Byte .
    char ch = 'S' ;

    bool bl = true ;

    float fl = 1.3;

    int dl  = 1.23 ;
    int size = sizeof(a);

    int c = 'A' ;

    // can use [ endl] ,['\n'] for the next line
    cout << size << endl ;
    cout << a << '\n'  ;
    cout << ch << endl ;
    cout << bl << '\n'  ;
    cout << fl  << endl  ;
    cout << dl << endl ;
    cout << c << '\n' ;


}
